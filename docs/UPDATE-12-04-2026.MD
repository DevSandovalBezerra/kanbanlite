# Registro de Alterações — 12/04/2026

## Resumo
- Correção em Secrets por projeto: `title` e `description` voltam a ser persistidos, listados e recuperados na edição.
- Ajuste de compatibilidade para evitar warnings em ambientes que ainda estejam com DTO antigo em cache.

## Secrets — Correções
- Persistência:
  - Detecção de colunas `title` e `description` no MySQL passa a usar `SHOW COLUMNS`, evitando falhas por variações de case no PDO/driver.
  - Leitura de `title`/`description` no repositório passa a ser tolerante (sem acessar propriedades inexistentes) para compatibilidade com deploy parcial/opcache.

## Verificação
- `php -l app/Repositories/PdoProjectSecretRepository.php`

## Arquivos Alterados (principais)
- `app/Repositories/PdoProjectSecretRepository.php`
- `docs/UPDATE-12-04-2026.MD`
